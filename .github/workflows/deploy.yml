name: Deploy (OTAP) - proef v2-pages-fix

on:
  push:
    branches: [main, acceptatie, development]
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  otap:
    uses: HansdeRooijPrive/OTAP-CI/.github/workflows/deploy.yml@v2-pages-fix
